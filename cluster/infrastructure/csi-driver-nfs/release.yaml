apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: csi-driver-nfs
spec:
  releaseName: csi-driver-nfs
  chart:
    spec:
      chart: csi-driver-nfs
      version: '4.11.0'
      sourceRef:
        kind: HelmRepository
        name: csi-driver-nfs
      interval: '24h'
  interval: '24h'
  install:
    remediation:
      retries: 3
  timeout: '5m'
  values: {}
