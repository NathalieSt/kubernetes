apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: registry
  name: registry
spec:
  ports:
  - name: registry
    port: 5000
    targetPort: 5000
  selector:
    app.kubernetes.io/name: registry
