apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: gluetun-proxy
  name: gluetun-proxy
spec:
  ports:
  - name: gluetun-proxy
    port: 8888
    targetPort: 8888
  selector:
    app.kubernetes.io/name: gluetun-proxy
