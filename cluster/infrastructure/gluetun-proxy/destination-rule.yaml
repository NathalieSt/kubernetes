apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: gluetun-tcp
spec:
  host: gluetun-proxy.gluetun-proxy.svc.cluster.local
  trafficPolicy:
    tls:
      mode: DISABLE  # Critical for proxy traffic
    connectionPool:
      tcp:
        maxConnections: 100