apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: csi-driver-smb
spec:
  releaseName: csi-driver-smb
  chart:
    spec:
      chart: csi-driver-smb
      version: '*'
      sourceRef:
        kind: HelmRepository
        name: csi-driver-smb
      interval: '24h'
  interval: '24h'
  install:
    remediation:
      retries: 3
  timeout: '5m'
  values: {}
