---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: netbird-mgmt-api-key-vault-secret
spec:
  vaultAuthRef: vault-auth
  mount: kvv2
  type: kv-v2
  path: netbird/mgmt-api-key
  refreshAfter: 24h
  destination:
    create: true
    name: netbird-mgmt-api-key
