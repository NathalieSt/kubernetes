---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: caddy
    app.kubernetes.io/version: 2.10.0-alpine
  name: caddy
spec:
  selector:
    app.kubernetes.io/name: caddy
  ports:
  - name: http-caddy
    port: 80
    targetport: 80
