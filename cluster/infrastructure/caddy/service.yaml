apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: caddy
    app.kubernetes.io/version: '2.10.0-alpine'
  name: caddy
spec:
  ports:
  - name: http
    port: 80
    targetPort: 80
  selector:
    app.kubernetes.io/name: caddy
