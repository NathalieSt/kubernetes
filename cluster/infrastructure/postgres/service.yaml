apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/version: '17.6'
  name: postgres
spec:
  ports:
  - name: tcp-postgres
    port: 5432
    targetPort: 5432
  selector:
    app.kubernetes.io/name: postgres
