apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-service-access
spec:
  action: ALLOW
  rules:
  - from:
    - source:
        namespaces: ["forgejo"]
    to:
    - operation:
        hosts: ["postgres-rw.postgres.svc.cluster.local"]