apiVersion: v1
data:
  init-wikijs.sql: "\n            SELECT 'CREATE DATABASE wikijs'\n            WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'wikijs')\\gexec\n        \n        "
kind: ConfigMap
metadata:
  name: init-configmap
