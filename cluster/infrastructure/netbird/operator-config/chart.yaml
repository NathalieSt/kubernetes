---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmChart
metadata:
  name: operator-config-chart
spec:
  interval: 24h
  chart: netbird-operator-config
  reconcileStrategy: ChartVersion
  sourceRef:
    kind: HelmRepository
    name: operator-config-repo
