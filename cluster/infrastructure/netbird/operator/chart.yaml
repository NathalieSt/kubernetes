---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmChart
metadata:
  name: netbird-operator-chart
spec:
  interval: 24h
  chart: kubernetes-operator
  reconcileStrategy: ChartVersion
  sourceRef:
    kind: HelmRepository
    name: netbird-operator-repo
  version: 0.1.15
