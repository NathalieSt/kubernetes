---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmChart
metadata:
  name: operator-chart
spec:
  interval: 24h
  chart: kubernetes-operator
  reconcileStrategy: ChartVersion
  sourceRef:
    kind: HelmRepository
    name: operator-repo
