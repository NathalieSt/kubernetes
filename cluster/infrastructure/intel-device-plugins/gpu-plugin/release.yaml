---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: intel-device-gpu-plugin
spec:
  interval: 24h
  chartRef:
    kind: HelmChart
    name: intel-device-gpu-plugin-chart
  timeout: 10m
  releaseName: intel-device-gpu-plugin
  install:
    remediation:
      retries: 3
