apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: vault-auth
spec:
  kubernetes:
    role: nginx
    serviceAccount: nginx-vault-serviceaccount
  vaultAuthGlobalRef:
    allowDefault: true
    namespace: vault-secrets-operator
  vaultConnectionRef: "vault-secrets-operator/default"