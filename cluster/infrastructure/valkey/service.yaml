apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: valkey
  name: valkey
spec:
  ports:
  - name: valkey
    port: 6379
    targetPort: 6379
  selector:
    app.kubernetes.io/name: valkey
