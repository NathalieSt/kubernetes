apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    app.kubernetes.io/name: valkey
  name: valkey-pvc
spec:
  resources:
    requests:
      storage: '10Gi'
  storageClassName: nfs-client
