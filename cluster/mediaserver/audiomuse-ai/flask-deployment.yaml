---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: audiomuse-ai-flask
    app.kubernetes.io/version: sha256_9881304597ce7
  name: audiomuse-ai-flask
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: audiomuse-ai-flask
  template:
    metadata:
      labels:
        app.kubernetes.io/name: audiomuse-ai-flask
        app.kubernetes.io/version: sha256_9881304597ce7
    spec:
      containers:
      - image: ghcr.io/neptunehub/audiomuse-ai@sha256:9881304597ce7ea4938b670509e227a97aab42dcf8ca349139cebab321f2dd33
        name: audiomuse-ai-flask
        ports:
        - containerPort: 8000
          name: flask
        volumeMounts:
        - mountPath: /app/temp_audio
          name: temp-volume
        env:
        - name: SERVICE_TYPE
          value: flask
        - name: CLAP_ENABLED
          value: "false"
        - name: JELLYFIN_USER_ID
          valueFrom:
            secretKeyRef:
              key: userid
              name: jellyfin-creds
        - name: JELLYFIN_TOKEN
          valueFrom:
            secretKeyRef:
              key: token
              name: jellyfin-creds
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              key: username
              name: postgres-creds-secret
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              key: password
              name: postgres-creds-secret
        envFrom:
        - configMapRef:
            name: audiomuse-configmap
      volumes:
      - name: temp-volume
        persistentVolumeClaim:
          claimName: audiomuse-ai-pvc
