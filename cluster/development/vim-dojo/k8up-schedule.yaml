---
apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: vim-dojo-backup
  namespace: vim-dojo
spec:
  backend:
    repoPasswordSecretRef:
      name: restic-repo-password-secret
      key: password
    s3:
      endpoint: http://garage.garage.svc.cluster.local:3900
      bucket: kubernetes-backups-vim-dojo
      accessKeyIDSecretRef:
        name: garage-backup-access-key-secret
        key: access-key-id
      secretAccessKeySecretRef:
        name: garage-backup-access-key-secret
        key: secret-access-key
  backup:
    schedule: 0 3 * * *
    podSecurityContext:
      runAsUser: 0
      runAsGroup: 0
      seccompProfile:
        type: RuntimeDefault
    podConfigRef:
      name: vim-dojo-backup-pod
  prune:
    schedule: 0 5 * * 0
    retention:
      keepLast: 14
      keepWeekly: 8
---
apiVersion: k8up.io/v1
kind: PodConfig
metadata:
  name: vim-dojo-backup-pod
  namespace: vim-dojo
spec:
  template:
    spec:
      containers:
      - name: backup
        securityContext:
          capabilities:
            drop:
            - ALL
          allowPrivilegeEscalation: false
