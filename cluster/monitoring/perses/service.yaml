apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: perses
    app.kubernetes.io/version: v0.52
  name: perses
spec:
  ports:
  - name: http-perses
    port: 8080
    targetPort: 8080
  selector:
    app.kubernetes.io/name: perses
