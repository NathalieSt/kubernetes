---
apiVersion: netbird.io/v1alpha1
kind: NetworkResource
metadata:
  name: perses
  namespace: perses
spec:
  networkRouterRef:
    name: netbird-operator-config
    namespace: netbird
  serviceRef:
    name: perses
