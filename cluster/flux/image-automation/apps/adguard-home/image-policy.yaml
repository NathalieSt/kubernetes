apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: adguard-home-image-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: adguard-home-image-repository
  policy:
    semver:
      range: x.x.x