apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: nfs-subdir-external-provisioner-image-repository
  namespace: flux-system
spec:
  image: nfs-subdir-external-provisioner/nfs-subdir-external-provisioner
  interval: 5m