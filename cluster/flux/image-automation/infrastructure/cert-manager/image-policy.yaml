apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: cert-manager-image-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: cert-manager-image-repository
  policy:
    semver:
      range: x.x.x
