apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gitea-web-ingress
spec:
  ingressClassName: tailscale
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gitea-web-service
                port:
                  number: 3000
  tls:
    - hosts:
        - gitea
