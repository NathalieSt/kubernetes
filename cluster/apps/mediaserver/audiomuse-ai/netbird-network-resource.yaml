---
apiVersion: netbird.io/v1alpha1
kind: NetworkResource
metadata:
  name: audiomuse-ai
  namespace: jellyfin
spec:
  networkRouterRef:
    name: netbird-operator-config
    namespace: netbird
  serviceRef:
    name: audiomuse-ai
