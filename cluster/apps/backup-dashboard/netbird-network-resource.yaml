---
apiVersion: netbird.io/v1alpha1
kind: NetworkResource
metadata:
  name: backup-dashboard
  namespace: backup-dashboard
spec:
  networkRouterRef:
    name: k8s-network
    namespace: netbird
  serviceRef:
    name: backup-dashboard
  groups:
  - name: kubernetes
