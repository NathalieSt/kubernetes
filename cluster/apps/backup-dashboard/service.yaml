---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: backup-dashboard
  name: backup-dashboard
spec:
  selector:
    app.kubernetes.io/name: backup-dashboard
  ports:
  - name: backup-dashboard
    port: 8080
    targetPort: 8080
