apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: conduwuit-ingress
spec:
  ingressClassName: tailscale
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: conduwuit
            port:
              number: 8200
  tls:
  - hosts:
    - conduwuit
