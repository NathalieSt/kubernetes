apiVersion: v1
kind: Service
metadata:
  name: conduwuit-service
spec:
  type: ClusterIP
  selector:
    app: conduwuit
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

