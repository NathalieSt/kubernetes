apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: conduwuit
spec:
  releaseName: conduwuit
  chart:
    spec:
      chart: conduwuit
      version: "0.1.0"
      sourceRef:
        kind: HelmRepository
        name: conduwuit
      interval: "5m"
  interval: "50m"
  install:
    remediation:
      retries: 3
  values:
