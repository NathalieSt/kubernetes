---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lidify-config-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
  storageClassName: local-path
