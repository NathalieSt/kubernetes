apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: lidify-policy
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: netbird-router
  egress:
