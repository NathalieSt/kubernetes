apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all-traffic
spec:
  podSelector: {}
  policyTypes:
    ingress:
      - from:
          - podSelector:
              matchLabels:
                app.kubernetes.io/name: netbird-router
    egress:
