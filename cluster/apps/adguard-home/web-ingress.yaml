apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adguard-home-web-ingress
spec:
  ingressClassName: tailscale
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: adguard-home-web-service
                port:
                  number: 3000
  tls:
    - hosts:
        - adguard-home-web
