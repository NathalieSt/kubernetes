apiVersion: v1
kind: Service
metadata:
  name: adguard-home-install-service
  namespace: adguard-home
spec:
  type: ClusterIP
  selector:
    app: adguard-home
  ports:
    - port: 3000
      targetPort: 3000
