apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: tandoor
    app.kubernetes.io/version: '2.0.1'
  name: tandoor
spec:
  ports:
  - name: http-tandoor
    port: 8080
    targetPort: 8080
  selector:
    app.kubernetes.io/name: tandoor
