apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/name: tandoor
    app.kubernetes.io/version: '2.0.1'
  name: media-files-tandoor-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: '5Gi'
  storageClassName: nfs-remote
