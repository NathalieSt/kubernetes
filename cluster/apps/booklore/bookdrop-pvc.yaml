---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bookdroppvc-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-local-v2
  volumeName: data-pv
