apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ampache-ingress
spec:
  ingressClassName: tailscale
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ampache-service
                port:
                  number: 80
  tls:
    - hosts:
        - ampache
