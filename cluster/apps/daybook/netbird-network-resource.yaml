---
apiVersion: netbird.io/v1alpha1
kind: NetworkResource
metadata:
  name: daybook
  namespace: daybook
spec:
  networkRouterRef:
    name: k8s-network
    namespace: netbird
  serviceRef:
    name: daybook
  groups:
  - name: kubernetes
