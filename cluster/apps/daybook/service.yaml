---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: daybook
    app.kubernetes.io/version: bec2af8
  name: daybook
spec:
  selector:
    app.kubernetes.io/name: daybook
  ports:
  - name: http
    port: 8080
    targetPort: 8080
