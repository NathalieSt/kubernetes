apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: invidious-ingress
spec:
  ingressClassName: tailscale
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: invidious
            port:
              number: 8096
  tls:
  - hosts:
    - invidious
