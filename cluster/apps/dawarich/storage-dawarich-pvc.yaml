apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storage-dawarich-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: '10Gi'
  storageClassName: nfs-client
