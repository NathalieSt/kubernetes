apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: dawarich
    app.kubernetes.io/version: latest
  name: dawarich
spec:
  ports:
  - name: http
    port: 3000
    targetPort: 3000
  selector:
    app.kubernetes.io/name: dawarich
