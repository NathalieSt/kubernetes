apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: dawarich
  name: dawarich
spec:
  ports:
  - name: dawarich
    port: 3000
    targetPort: 3000
  selector:
    app.kubernetes.io/name: dawarich
