---
apiVersion: netbird.io/v1alpha1
kind: NetworkResource
metadata:
  name: it-tools
  namespace: it-tools
spec:
  networkRouterRef:
    name: k8s-network
    namespace: netbird
  serviceRef:
    name: it-tools
  groups:
  - name: kubernetes
