---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: affine
    app.kubernetes.io/version: v0.25.7
  name: affine
spec:
  selector:
    app.kubernetes.io/name: affine
  ports:
  - name: http-affine
    port: 3010
    targetPort: 3010
