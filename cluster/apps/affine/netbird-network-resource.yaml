---
apiVersion: netbird.io/v1alpha1
kind: NetworkResource
metadata:
  name: affine
  namespace: affine
spec:
  networkRouterRef:
    name: k8s-network
    namespace: netbird
  serviceRef:
    name: affine
  groups:
  - name: kubernetes
