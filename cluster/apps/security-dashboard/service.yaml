---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: security-dashboard
    app.kubernetes.io/version: latest
  name: security-dashboard
spec:
  selector:
    app.kubernetes.io/name: security-dashboard
  ports:
  - name: http
    port: 8080
    targetPort: 8080
