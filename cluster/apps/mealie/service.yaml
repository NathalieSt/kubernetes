apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: mealie
    app.kubernetes.io/version: v3.0.2
  name: mealie
spec:
  ports:
  - name: mealie
    port: 9000
    targetPort: 9000
  selector:
    app.kubernetes.io/name: mealie
