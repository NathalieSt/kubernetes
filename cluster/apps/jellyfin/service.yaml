apiVersion: v1
kind: Service
metadata:
  annotations:
    netbird.io/expose: "true"
    netbird.io/resource-name: jellyfin
    netbird.io/groups: jellyfin, k3s-apps
  labels:
    app.kubernetes.io/name: jellyfin
  name: jellyfin-service
spec:
  ports:
  - name: http
    port: 8096
    protocol: TCP
    targetPort: 8096
  selector:
    app.kubernetes.io/name: jellyfin
  type: ClusterIP