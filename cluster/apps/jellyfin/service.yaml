apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: jellyfin
  name: jellyfin-service
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8096
  selector:
    app.kubernetes.io/name: jellyfin
  type: ClusterIP