apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyfin-library-storage-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: '100Gi'
  storageClassName: nfs-client
