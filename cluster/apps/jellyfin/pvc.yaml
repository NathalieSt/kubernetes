apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyfin-library-storage-pvc
  namespace: jellyfin
spec:
  storageClassName: "nfs-client"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "100Gi"