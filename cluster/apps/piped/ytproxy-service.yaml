apiVersion: v1
kind: Service
metadata:
  annotations:
    netbird.io/expose: "true"
    netbird.io/resource-name: ytproxy
    netbird.io/groups: k3s-apps,piped
  labels:
    app: ytproxy-service
  name: ytproxy-service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    protocol: TCP
  