apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: example-virtualservice
spec:
  hosts:
    - "example.com"
  gateways:
    - cluster-gateway
  http:
    - route:
        - destination:
            host: gatus
            port:
              number: 80