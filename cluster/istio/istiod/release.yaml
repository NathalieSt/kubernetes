apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: istiod
spec:
  releaseName: istiod
  chart:
    spec:
      chart: istiod
      version: '*'
      sourceRef:
        kind: HelmRepository
        name: istio-repo
      interval: '24h'
  interval: '24h'
  install:
    remediation:
      retries: 3
  timeout: '5m'
  values: {}
