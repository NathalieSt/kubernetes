---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: istiod
spec:
  interval: 24h
  chartRef:
    kind: HelmChart
    name: istiod-chart
  timeout: 10m
  releaseName: istiod
  install:
    remediation:
      retries: 3
