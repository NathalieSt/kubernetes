---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmChart
metadata:
  name: base-chart
spec:
  interval: 24h
  chart: base
  reconcileStrategy: ChartVersion
  sourceRef:
    kind: HelmRepository
    name: istio-repo
