---
apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: radicale-backup
  namespace: radicale
spec:
  backend:
    repoPasswordSecretRef:
      name: restic-repo-password-secret
      key: password
    s3:
      endpoint: http://garage.garage.svc.cluster.local:3900
      bucket: kubernetes-backups-radicale
      accessKeyIDSecretRef:
        name: garage-backup-access-key-secret
        key: access-key-id
      secretAccessKeySecretRef:
        name: garage-backup-access-key-secret
        key: secret-access-key
  backup:
    schedule: 0 3 * * *
    podSecurityContext:
      runAsUser: 0
      runAsGroup: 0
      seccompProfile:
        type: RuntimeDefault
    podConfigRef:
      name: radicale-backup-pod
  prune:
    schedule: 0 5 * * 0
    retention:
      keepLast: 14
      keepWeekly: 8
      keepMonthly: 6
---
apiVersion: k8up.io/v1
kind: PodConfig
metadata:
  name: radicale-backup-pod
  namespace: radicale
spec:
  template:
    spec:
      containers:
      - name: backup
        securityContext:
          capabilities:
            drop:
            - ALL
          allowPrivilegeEscalation: false
